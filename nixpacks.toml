[phases.setup]
nixPkgs = ['python312', 'postgresql']

[phases.install]
cmds = ['python -m pip install --upgrade pip', 'python -m pip install -r requirements.txt']

[phases.build]
cmds = ['echo "Build phase - no collectstatic needed"']

[start]
cmd = 'cd cardapioAPIProject && python manage.py migrate && gunicorn --bind 0.0.0.0:$PORT cardapioAPI.wsgi:application'
